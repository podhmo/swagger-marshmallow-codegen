definitions:
  wrap:
    type: object
    properties:
      empties:
        $ref: '#/definitions/wrapEmpties'
  wrapEmpties:
    x-marshmallow-inline: wrap
    type: array
    items:
      $ref: '#/definitions/wrapEmptiesItem'
  wrapEmptiesItem:
    x-marshmallow-inline: wrap
    type: object
    title: empty
