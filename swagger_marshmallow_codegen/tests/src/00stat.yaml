# array ref with primitive type
definitions:
  stat:
    properties:
      days:
        $ref: "#/definitions/days"
      days2:
        $ref: "#/definitions/days2"
      total:
        type: integer
      week:
        type: integer
  days:
    type: array
    items:
      type: integer
  days2:
    $ref: "#/definitions/days"
