definitions:
  person0:
    description: original (no. 1)
    properties:
      name:
        type: string
  person:
    allOf:
      - $ref: "#/definitions/person0"
      - required: ["name"]
      - description: no. 3
      - properties:
          age:
            type: integer
